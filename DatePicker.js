'use strict';
class DatePicker{
    constructor(id, callBack) {
    this.id = id;
    this.callBack = callBack;

    }
    render(date){
        const div = datepicker.html.getElementById(this.id);
    }
}